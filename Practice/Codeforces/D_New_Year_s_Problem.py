def isPossible(a, m, n, x):
    extraFound = 0
    visitedShops = set()
    for i in range()
def solve():
    input()
    m,n = map(int,input().split())
    a = []
    maxval = 0
    minval = 100000
    for _ in range(m):
        k=list(map(int,input().split()))
        maxval = max(maxval, max(k))
        minval = min(minval, min(k))
        a+=[k]
    l,r = minval,maxval
    # while(l<=r):



for _ in range(int(input())):
    solve()